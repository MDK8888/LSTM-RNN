# LSTM-RNN
## Project Overview
This is my implementation of a Long-Short Term Memory(LSTM) Recurrent Neural Network(RNN) used for Time Series Prediction. It was built and trained entirely in Numpy without the aid of Tensorflow/Keras.
